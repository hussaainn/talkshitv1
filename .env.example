NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy name, also accepted by src/lib/supabase.js:
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# AI referee gateway (OpenAI-compatible). Key stays server-side, used only
# by src/app/api/referee/route.js — never exposed to the client.
AI_API=
# AI gateway endpoint + model. Defaults work for OpenRouter-style gateways.
# AI_BASE_URL=https://openrouter.ai/api/v1
# AI_MODEL=nex-agi/nex-n2.5-pro:free
